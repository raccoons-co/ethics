{
  "name": "cleanway-skeleton",
  "version": "0.2.1",
  "description": "Write YourTest cases, fail fast and have a nice journey in the Node.js galaxy.",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "node dist/test/EntryPoint"
  },
  "devDependencies": {
    "@raccoons-co/cleanway": "^0.2.1",
    "@types/chai": "^4.3.4",
    "chai": "^4.3.7",
    "typescript": "^4.9.5"
  }
}
